{
  "devDependencies": {
    "gts": "^5.0.1",
    "typescript": "~5.1.6",
    "@types/node": "20.4.6"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
